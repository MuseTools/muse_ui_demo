name: muse_ui_workspace

repository:
  type: github
  origin: https://github.com
  owner: MuseTools
  name: muse_ui

packages:
  - "apps/**"
  - "packages/**"

command:
  version:
    workspaceChangelog: false

scripts:
  analyze:
    exec: dart analyze .
